\section{Модификация ИС ОПСАПК на основе автоматизированного выбора модели источника ошибок.} \label{sect3_2}
\subsection{Схема модифицированной ИС ОПСАПК.}
Рассмотрим ИС ОПСАПК, база источников ошибок в которой содержит только модели из класса скрытых полумарковских моделей. Расширим БУИМ из ИМ ЦПК (см. рис. \ref{Deundyak-fig1}) модулем подбора математической модели источника ошибок. Этот модуль получает на вход поток ошибок, зарегистрированный в реальном канале связи. Далее на основе алгоритма, предложенного в разделе 3.999 (вход, выход, ...), модуль выбирает из базы моделей источников ошибок, находящейся в БИВВ, ту модель, которая генерирует потоки ошибок, наиболее близкие к полученному.
Расширенный таким образом БУИМ будем обозначать БУИМ(А).

Выбранная модулем подбора математической модели источника ошибок модель подается на блок БУИМ. БУИМ делает запрос в БОР о результатах экспериментов по исследованию и подбору средств помехоустойчивой защиты для выбранной модели. Если такие эксперименты не проводились, то выбранная модель подается на вход блока БИВВ, который осуществляет генерацию потоков ошибок с помощью этой модели в рамках имитационных экспериментов (см. раздел 1.1.999).

Модель источника ошибок может выбираться как из всего множества базовых моделей, так и из некоторого его подмножества, выбранного на основе каких-либо дополнительных соображений.

Заметим, что в работе [найти работу про окно] предлагается метод определения числа различных состояний частного случая скрытой полумарковской модели по последовательности наблюдений. Используя идеи работы [работы про окно], представляется возможным сузить число исследуемых базовых моделей до моделей с определенным числом состояний. Однако, в настоящей работе это вопрос не исследовался.

\begin{figure}[h]%пример использования рисунка
  \centering
\includegraphics[scale=0.2]{/part4/isopsapk_mod.jpg}
  \caption{Модифицированная схема ИС ОПСАПК}\label{Deundyak-fig3}
\end{figure}

Отметим, что при моделировании канала связи в ИС ОПСАПК помехоустойчивые кодеки, а также модели потока ошибок выбираются из набора доступных кодеков и моделей в системе. Схематично это представлено на рис. \ref{Deundyak-fig3}.

\section{О компьютерной реализации модуля автоматизированного выбора модели источника ошибок.}
-- модуль, принимающий последовательность и IModelDataProvider, который в UI реализуется как выбор из базы, а в реализации ИС ОПСАПК будет свой другой
-- в старую реализацию не встроится модуль, но можно сделать новую, в которую встроится
\section{Пользовательский интерфейс для моделирования и анализа потоков ошибок}
НПС - мини-ИС

- НПС содержит программную реализацию модуля выбора модели и предлагается упрощенное обращение к нему

- исследователю моделей потоков ошибок НПС предоставляет возможность обратиться к модулю из главы 2 и сгенерировать последовательность ошибок моделью с заданными им параметрами для дальнейшего исследования сторонними средствами

- исследователю моделей потоков ошибок НПС предоставляет средства статистического анализа моделей источника ошибок на предмет соответствия заданным последовательностями, реализованные в модуле главы 3

- кто является пользователем НПС:

1) отладчик новой версии информационной системы (которая отличается от старой, по существу, только добавлением модуля подбора)

2) разработчик новой версии ИС ОПСАПК, отличающейся от старой новой базой моделей шума (новые модели источника ошибок), ...

3) разработчик новой версии ИС ОПСАПК, отличающейся от старой возможностью определять, достаточно ли хороша подобранная модель и строить новую (лучшую) на ее основе.

4) автор диссертации для имитации работы ИС ОПСАПК, отладки программных комплексов глав 2 и 3

5) потенциальный пользователь ИС, который хочет решать те задачи, которые умеет решать информационная система, может пользоваться в исследовательских целях только НПС.

%https://books.google.ru/books?id=0ZreBwAAQBAJ&pg=PA280&lpg=PA280&dq=Error+Source+Models+turin+pdf&source=bl&ots=gBZHsR7tks&sig=V1f-vqwXoo9C5hirla0Lykdwcyk&hl=ru&sa=X&ved=0ahUKEwjX5t2ZnJjYAhXmC5oKHZ7DC2gQ6AEIOTAC#v=onepage&q&f=false


%\section{"Пример" работы блока подбора модели} \label{sect2_4}
